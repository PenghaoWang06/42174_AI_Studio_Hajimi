name: snapfolio_frontend
description: Flutter frontend for the SnapFolio business card scanner.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  camera: ^0.12.0+1
  flutter:
    sdk: flutter
  http: ^1.2.2
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
