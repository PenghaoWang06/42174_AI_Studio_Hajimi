# Backend Model

The backend loads the model from this fixed path:

```text
backend/models/model.pt
```

When replacing the model, copy the new YOLO model file into this folder and rename it to:

```text
model.pt
```

Do not change the file name unless the backend config is also updated.
